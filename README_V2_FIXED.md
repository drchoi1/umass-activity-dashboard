# UMass Activity Dashboard v2 — fixed

Replace the matching files in your existing repo with this package, commit/push, then manually run **Actions → Update UMass Activity Dashboard → Run workflow** once.

Confirm that both **Build dashboard** and **Commit generated page** are green. The workflow will replace the placeholder `index.html` with the live dashboard and push that generated page back to `main`.

GitHub Pages can take a minute or two to publish the resulting commit.
